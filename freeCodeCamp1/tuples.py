#tuple is similar to a list with a few key differences
#tuples are immutable
# used in cases where you dont need to change the data

coordinates = (4, 5)
coordinates1 = [(4, 5),(10, 20),(15, 30)]
print(coordinates[1])
