import functions

# the function invocation seems to be run the entire file rather than the selected function. Maybe python 3 has a different syntax. Regardless I am sure I will have more opportunities to find out.

print(functions.cube(3))

# there are a lot of built in modules you can access through external libs.

#pip install name-of-the-doc
#pip is the package manager in python

